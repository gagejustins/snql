/**
 * PropertiesPlugin.ts
 * @copyright Microsoft 2018
 */
import { ITelemetryItem, IDiagnosticLogger } from '@microsoft/applicationinsights-core-js';
import { Session, _SessionManager } from './Context/Session';
import { ITelemetryContext, IOperatingSystem, IWeb } from '@microsoft/applicationinsights-common';
import { Application } from './Context/Application';
import { Device } from './Context/Device';
import { Internal } from './Context/Internal';
import { User } from './Context/User';
import { Location } from './Context/Location';
import { ITelemetryConfig } from './Interfaces/ITelemetryConfig';
import { TelemetryTrace } from './Context/TelemetryTrace';
export declare class TelemetryContext implements ITelemetryContext {
    application: Application;
    device: Device;
    location: Location;
    telemetryTrace: TelemetryTrace;
    user: User;
    internal: Internal;
    session: Session;
    sessionManager: _SessionManager;
    os: IOperatingSystem;
    web: IWeb;
    appId: () => string;
    constructor(logger: IDiagnosticLogger, defaultConfig: ITelemetryConfig);
    applySessionContext(event: ITelemetryItem): void;
    applyOperatingSystemContxt(event: ITelemetryItem): void;
    applyApplicationContext(event: ITelemetryItem): void;
    applyDeviceContext(event: ITelemetryItem): void;
    applyInternalContext(event: ITelemetryItem): void;
    applyLocationContext(event: ITelemetryItem): void;
    applyOperationContext(event: ITelemetryItem): void;
    applyWebContext(event: ITelemetryItem): void;
    applyUserContext(event: ITelemetryItem): void;
    cleanUp(event: ITelemetryItem): void;
}
